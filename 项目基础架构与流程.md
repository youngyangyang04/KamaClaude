# 项目基础架构与流程

> 本文提炼自「S0-项目基础架构」和「Loop Engineer 解析」两段学习会话，并结合
> ScottClaude 当前代码进行了校正。
>
> **时间边界：**会话中的 S0 是项目早期状态；截至 2026-07-29，仓库已经继续实现
> Agent Loop、LLM、工具、会话、事件、权限、Trace、上下文压缩和 MCP。本文会明确区分
> “S0 当时完成了什么”和“当前项目已经演进到哪里”。

---

## 1. 项目整体定位

ScottClaude 是一个本地双进程 Coding Agent 系统：

```text
用户
  ↓
scott-tui（主要界面） / scott（调试 CLI）
  ↓
TCP Loopback + NDJSON + JSON-RPC 2.0
  ↓
scott-core（常驻 daemon）
  ↓
Agent Runtime
  ├── LLM
  ├── Tools
  ├── Sessions
  ├── Events
  ├── Permissions
  ├── Trace
  └── MCP
```

- `scott-core` 是常驻执行中心，负责执行任务和保存运行状态。
- `scott-tui` 是主要产品界面。
- `scott` CLI 主要用于快速测试和调试。
- 客户端与 Core 不直接调用彼此的 Python 函数，而是通过本机 TCP 发送协议消息。

---

## 2. S0 做了什么

### 2.1 S0 的目标

S0 没有让 Agent 真正执行 Coding 任务，而是先搭好后续能力共同依赖的地基：

```text
项目启动
  + 配置加载
  + 日志
  + 双进程结构
  + TCP 通信
  + JSON-RPC 协议
  + Pydantic 校验
  + 测试脚手架
  + 协议文档生成
```

它像一间已经接好电源和网络的空房间：线路已经通了，但早期还没有接入真正工作的机器。

### 2.2 S0 已完成的基础能力

| 能力 | 作用 |
|---|---|
| Python 项目脚手架 | 统一源码目录、依赖和构建方式 |
| `pyproject.toml` 入口 | 安装后可运行 `scott`、`scott-core`、`scott-tui` |
| `ScottConfig` | 开发、测试、部署共享同一套配置模型 |
| CLI + daemon | 客户端与常驻 Core 分离 |
| TCP Loopback | 通过本机 `127.0.0.1:7437` 通信 |
| NDJSON | 用换行符区分一条条 JSON 消息 |
| JSON-RPC 2.0 | 定义请求、响应、错误和方法路由 |
| Pydantic 模型 | 在协议边界校验和序列化数据 |
| `WIRE_PROTOCOL.md` | 从模型生成协议文档，减少代码与文档不一致 |
| 单元/集成测试 | 验证模型、配置、Socket 和真实 daemon 往返 |

### 2.3 S0 当时的边界

会话描述的 S0 时点，daemon 主要只能回答：

```text
core.ping
```

当时还没有：

- 调用 LLM；
- Agent 执行循环；
- 工具注册与执行；
- 真正的事件流订阅；
- 会话持久化；
- 权限审批；
- MCP。

所以图片中的总结是准确的：S0 的价值不在于“Agent 已经能干活”，而在于后续能力有了稳定地基。

---

## 3. 项目入口与技术栈

### 3.1 三个命令入口

`pyproject.toml` 定义：

```toml
[project.scripts]
scott = "scott_claude.cli.main:main"
scott-core = "scott_claude.core.app:run"
scott-tui = "scott_claude.tui.__main__:main"
```

对应关系：

| 终端命令 | Python 入口 |
|---|---|
| `scott` | `scott_claude.cli.main.main()` |
| `scott-core` | `scott_claude.core.app.run()` |
| `scott-tui` | `scott_claude.tui.__main__.main()` |

执行：

```bash
uv run scott-core
```

本质上会进入：

```python
def run() -> None:
    asyncio.run(CoreApp().run())
```

### 3.2 主要技术栈

| 分类 | 技术 |
|---|---|
| 语言与运行时 | Python 3.12、`asyncio` |
| 数据模型 | Pydantic v2、`dataclass`、类型提示 |
| LLM | Anthropic Python SDK |
| 网络 | TCP Loopback、asyncio Streams、HTTPX |
| IPC 协议 | JSON-RPC 2.0 + NDJSON |
| 终端界面 | Textual、Rich Markdown |
| 配置 | TOML、`.env`、环境变量 |
| 扩展协议 | MCP |
| 工程工具 | uv、Hatchling、Ruff、Mypy、Pytest |

ScottClaude 没有依赖 LangChain、CrewAI 或 AutoGen 来实现 Agent 核心循环，而是自研
Agent Runtime，便于学习底层原理并精确控制状态、权限和协议。

---

## 4. CLI、子命令与配置

### 4.1 `argparse` 子命令

```text
scott                  主命令
scott --version        可选参数
scott ping             子命令
```

可以这样记：

```text
子命令：告诉程序“做什么”
可选参数：告诉程序“怎么做”
```

示例：

```python
parser = argparse.ArgumentParser(prog="scott")
subparsers = parser.add_subparsers(dest="command")
subparsers.add_parser("ping")
args = parser.parse_args()
```

执行：

```bash
scott ping
```

会得到：

```python
args.command == "ping"
```

`subparsers` 就是“子命令解析器集合”，`dest="command"` 表示将选中的子命令保存到
`args.command`。

### 4.2 `get_config()` 的配置合并

默认加载路径：

```text
ScottConfig 默认值
        ↓
~/.scott/config.toml（全局）
        ↓
.scott/config.toml（项目）
        ↓
.env
        ↓
系统环境变量
        ↓
最终 ScottConfig
```

优先级：

```text
系统环境变量 > .env > 项目 TOML > 全局 TOML > 默认值
```

`load_dotenv(".env", override=False)` 表示 `.env` 不覆盖系统中已经存在的同名环境变量。

如果设置：

```env
SCOTT_CONFIG=./custom.toml
```

则只读取这个指定 TOML，不再叠加默认的全局和项目 TOML：

```text
系统环境变量 > .env > custom.toml > 默认值
```

TOML 的转换过程：

```text
TOML 文件
  ↓ tomllib.load()
Python 字典
  ↓ _apply_toml()
ScottConfig 对象
  ↓ _apply_env()
最终配置
```

如果 TOML 格式错误，代码通过 `SystemExit` 停止启动，避免 daemon 带着错误配置继续运行。

---

## 5. 异步编程基础

### 5.1 协程函数、协程对象和 Task

```python
async def ping() -> None:
    print("ping")
```

- `ping`：协程函数；
- `ping()`：调用后得到协程对象，函数体尚未真正执行；
- `await ping()`：在异步函数中执行并等待；
- `asyncio.run(ping())`：从同步入口进入异步世界；
- `asyncio.create_task(ping())`：交给事件循环并发调度。

关系：

```text
async def 定义
  ↓
协程函数
  ↓ 调用
协程对象
  ↓ await / asyncio.run / create_task
真正执行
```

最容易记的区别：

```text
同步代码进入异步代码：asyncio.run()
异步代码等待异步代码：await
```

### 5.2 Event Loop

asyncio Event Loop 是异步任务调度器，负责：

- 等待网络 I/O；
- 恢复可以继续执行的协程；
- 调度 Task；
- 处理超时、取消和信号。

遇到 `await` 时，当前协程可以暂时让出执行权，让事件循环处理其他连接和任务，而不是让整个
daemon 原地等待。

---

## 6. daemon 生命周期

S0 的最小 `CoreApp.run()` 流程：

```text
记录启动时间
  ↓
加载配置
  ↓
初始化日志
  ↓
创建 SocketServer
  ↓
注册 core.ping
  ↓
启动 TCP 监听
  ↓
等待 SIGINT / SIGTERM
  ↓
停止服务器
```

同步入口：

```python
def run() -> None:
    asyncio.run(CoreApp().run())
```

关闭控制：

```python
loop = asyncio.get_running_loop()
shutdown = asyncio.Event()

loop.add_signal_handler(signal.SIGINT, shutdown.set)
loop.add_signal_handler(signal.SIGTERM, shutdown.set)

await shutdown.wait()
```

含义：

```text
Ctrl+C / SIGINT ─┐
                 ├──→ shutdown.set()
SIGTERM ─────────┘
                         ↓
                shutdown.wait() 结束
                         ↓
                执行资源清理
```

`await shutdown.wait()` 只暂停当前 `CoreApp.run()`；事件循环仍能接受连接、读取请求和运行
handler。

部分 Windows 事件循环不支持 `add_signal_handler()`。捕获 `NotImplementedError` 能避免启动
失败，但清理代码最好放在 `finally` 中，保证主协程被取消时仍能关闭 Server、MCP 和 Trace。

---

## 7. `SocketServer.start()` 与缩进

核心意图：

```text
尝试连接 host:port
        ↓
连接成功 → 已有服务监听 → 拒绝启动第二个 Core
        ↓
连接失败 → 暂时无人监听
        ↓
asyncio.start_server() 真正启动服务器
```

### 7.1 端口探测

```python
_r, w = await asyncio.open_connection(self._host, self._port)
```

这里暂时作为客户端探测端口：

- `_r` 是 `StreamReader`，但不使用；
- `w` 是 `StreamWriter`，用于关闭探测连接。

连接成功后：

```python
w.close()
await w.wait_closed()
raise SystemExit(f"core already running at {self._host}:{self._port}")
```

严格来说，这只能证明“端口上已有 TCP 服务”，不能证明对方一定是 ScottClaude。

连接失败时：

```python
except (ConnectionRefusedError, OSError):
    pass
```

`pass` 表示忽略预期的连接失败，继续启动自己的服务器。

### 7.2 真正启动服务器

```python
self._server = await asyncio.start_server(
    self._handle_connection,
    host=self._host,
    port=self._port,
    limit=_MAX_LINE_BYTES,
)
```

这一步才真正：

1. 创建服务器 Socket；
2. 绑定地址与端口；
3. 开始监听；
4. 新连接到来时调用 `_handle_connection(reader, writer)`。

`limit=_MAX_LINE_BYTES` 限制按行读取时的缓冲区大小，防止异常客户端无限发送不换行数据。

### 7.3 为什么 `start_server()` 比 `pass` 少缩进一层

```python
async def start(self) -> str:
    try:
        ...
    except OSError:
        pass

    self._server = await asyncio.start_server(...)
    return ...
```

结构是：

```text
start()
├── try
│   └── 端口探测
├── except
│   └── pass
├── start_server
└── return
```

`pass` 属于 `except`，所以多缩进一层；`start_server()` 属于函数主体，因此与 `try`、`except`
对齐。

工程上还要注意：

- “先探测、再绑定”之间存在很短的竞争窗口，最终应以 `start_server()` 的绑定结果为准；
- `ConnectionRefusedError` 本身是 `OSError` 的子类；
- 捕获整个 `OSError` 会同时包含地址无效等非预期问题，可以进一步细化。

---

## 8. TCP、NDJSON 与 JSON-RPC

### 8.1 Reader 与 Writer

建立连接：

```python
reader, writer = await asyncio.open_connection(host, port)
```

```text
reader：读取对方发送的数据
writer：向对方发送数据
```

发送：

```python
writer.write(data)
await writer.drain()
```

- `write()` 把字节放入发送缓冲区；
- `drain()` 在缓冲区拥堵时等待；
- `drain()` 不代表服务器已经处理完请求。

接收：

```python
line = await reader.readline()
```

关闭：

```python
writer.close()
await writer.wait_closed()
```

### 8.2 JSON-RPC 与 NDJSON 分工

```text
JSON-RPC 2.0：定义一条消息有哪些字段、如何表达成功和失败
NDJSON：定义连续 TCP 字节流中，一条消息在哪里结束
```

ScottClaude 规定：

```text
每一行 = 一条完整 JSON 消息
```

发送转换：

```text
Python 字典
  ↓ json.dumps()
JSON 字符串
  ↓ + "\n"
NDJSON 消息
  ↓ encode()
bytes
  ↓ writer.write()
TCP 字节流
```

JSON-RPC 请求：

```json
{
  "jsonrpc": "2.0",
  "id": "cli-1",
  "method": "core.ping",
  "params": {
    "client": "cli/0.0.1"
  }
}
```

成功响应：

```json
{
  "jsonrpc": "2.0",
  "id": "cli-1",
  "result": {
    "server_version": "0.0.1",
    "uptime_ms": 1520
  }
}
```

错误响应：

```json
{
  "jsonrpc": "2.0",
  "id": "cli-1",
  "error": {
    "code": -32601,
    "message": "Method not found"
  }
}
```

标准错误码：

| 错误码 | 含义 | 发生阶段 |
|---:|---|---|
| `-32700` | Parse error | 不是合法 JSON |
| `-32600` | Invalid Request | JSON 合法，但请求结构错误 |
| `-32601` | Method not found | 未注册对应 method |
| `-32602` | Invalid params | 参数缺失或类型错误 |
| `-32603` | Internal error | handler 内部异常 |

错误处理链：

```text
读取 bytes
  ├── JSON 解析失败 → -32700
  ↓
校验 JsonRpcRequest
  ├── 结构错误 → -32600
  ↓
查找 method
  ├── 未注册 → -32601
  ↓
校验 params
  ├── 参数错误 → -32602
  ↓
执行 handler
  ├── 内部异常 → -32603
  ↓
返回 JsonRpcSuccess
```

---

## 9. Pydantic、`method` 与 `type`

### 9.1 Pydantic 是带校验能力的数据模板

```python
class JsonRpcRequest(BaseModel):
    jsonrpc: Literal["2.0"] = "2.0"
    id: str
    method: str
    params: dict[str, Any] = Field(default_factory=dict)
```

含义：

- `jsonrpc` 只能是 `"2.0"`，不传时自动填默认值；
- `id` 和 `method` 是必填字符串；
- `params` 不传时，为当前实例创建一个独立空字典。

常用操作：

```python
request = JsonRpcRequest.model_validate(raw)  # 字典 → 校验后的对象
request.model_dump()                          # 对象 → Python 字典
request.model_dump_json()                     # 对象 → JSON 字符串
```

Pydantic 负责：

- 必填字段检查；
- 类型校验与有限转换；
- 默认值填充；
- 字典、对象与 JSON 之间的转换。

它负责数据边界，不负责 TCP 传输。

### 9.2 `method` 是 RPC 路由

服务端注册：

```python
server.register("core.ping", self._ping_handler)
```

内部近似：

```python
self._handlers["core.ping"] = self._ping_handler
```

请求到来后：

```python
handler = self._handlers.get(request.method)
result = await handler(request.params)
```

因此：

```text
method = 到 SocketServer 注册表中选择哪个 handler
```

注册时使用：

```python
self._ping_handler
```

而不是：

```python
self._ping_handler()
```

因为前者是传递函数本身，等请求到来时再调用；后者会立即执行函数。

### 9.3 `type` 是业务模型标签

```python
class PingCommand(BaseModel):
    type: Literal["core.ping"] = "core.ping"
    client: str
```

判别联合：

```python
Command = Annotated[
    PingCommand | AgentRunCommand | SessionCreateCommand | ...,
    Discriminator("type"),
]
```

概念上：

```text
type="core.ping"       → PingCommand
type="agent.run"       → AgentRunCommand
type="session.create"  → SessionCreateCommand
```

最核心的区别：

```text
method：传输/RPC 层选择 handler
type：模型/业务层标识具体数据类型
```

当前 SocketServer 实际使用 `method` 路由；进入 handler 后，再用明确的命令模型校验
`params`。许多 `type` 字段有默认值，所以线上 JSON 示例可以省略。当前 `method` 与 `type`
通常取相同值，在简单命令中看起来重复，但所属层次不同。

---

## 10. `core.ping` 完整流程

```text
用户执行 scott ping
  ↓
argparse 得到 command="ping"
  ↓
get_config()
  ↓
cmd_ping(config)
  ↓
asyncio.run(_ping(config))
  ↓
open_connection(host, port)
  ↓
发送 method="core.ping" 的 JSON-RPC 请求
  ↓
SocketServer._handle_connection()
  ↓
readline() 读取一条 NDJSON
  ↓
json.loads()
  ↓
JsonRpcRequest.model_validate()
  ↓
handlers["core.ping"]
  ↓
await CoreApp._ping_handler(params)
  ↓
返回 PongResult
  ↓
封装 JsonRpcSuccess
  ↓
model_dump_json() + "\n"
  ↓
写回客户端
  ↓
客户端校验 JsonRpcSuccess / PongResult
  ↓
打印 server version、uptime、latency
```

延迟计算：

```python
latency_ms = int((time.monotonic() - t0) * 1000)
```

如果计时在建立连接前开始，它包含：

```text
TCP 建连 + 请求发送 + Core 处理 + 响应返回
```

---

## 11. Event Loop、Agent Loop、Runtime 与 Workflow

### 11.1 三种 Loop

| Loop | 所属层 | 循环内容 |
|---|---|---|
| asyncio Event Loop | 系统运行层 | 调度协程、Task、网络 I/O、定时器 |
| Server 读取循环 | 传输层 | 等待消息 → 处理消息 → 继续等待 |
| Agent Loop | 业务层 | LLM → Tool → Observation → 再次 LLM |

它们可以嵌套存在：

```text
asyncio Event Loop
├── SocketServer 连接/读取循环
├── 请求 A
│   └── Agent Loop：LLM → Tool → Observation → LLM
├── 请求 B
│   └── core.ping：直接响应
└── Trace、事件、权限等待等后台任务
```

一句话区分：

```text
Event Loop：让异步程序运行和调度。
Server Loop：让服务器持续接收消息。
Agent Loop：让 Agent 持续思考、行动和观察。
```

### 11.2 Agent Loop

Agent Loop 的基本过程：

```text
调用 LLM（Plan）
  ↓
模型决定是否调用工具
  ↓
执行工具（Act）
  ↓
把结果加入上下文（Observe）
  ↓
再次调用 LLM
  ↓
直到成功、失败、取消或超过最大步数
```

当前 ScottClaude 是自研循环，核心近似：

```python
while not context.is_done():
    response = await provider.chat(...)

    if response.stop_reason == "tool_use":
        result = await invoke_tool(...)
        context.add_tool_result(...)
    elif response.stop_reason == "end_turn":
        context.mark_success()

    if context.step >= context.max_steps:
        context.mark_failed("exceeded_max_steps")
```

### 11.3 Loop Engineer

Loop Engineer 关注如何把人工反复操作自动化：

```text
执行 → 验证 → 发现失败 → 反馈 → 修正 → 再次执行
```

需要设计：

- 任务拆解；
- 状态保存；
- 自动测试或评估；
- 错误反馈；
- 最大轮数与超时；
- 取消和恢复；
- 人工审批；
- 可观测性。

### 11.4 Agent Runtime

Agent Runtime 是让 Agent 真正跑起来的执行引擎：

```text
LLM：提出下一步决策
Tools：执行读写文件、命令等动作
Runtime：控制循环、状态、权限、错误和终止
```

ScottClaude 的 Runtime 由多个模块协作：

```text
AgentRunner
  ↓
ExecutionContext
  ↓
AgentLoop
  ├── LLMProvider
  ├── ToolRegistry / invoke_tool
  ├── EventBus
  ├── PermissionManager
  └── Compactor
```

### 11.5 Workflow

Workflow 是提前定义好的任务流转步骤：

```text
接收任务
  ↓
创建/恢复 Session
  ↓
进入 Agent Runtime
  ↓
保存事件和结果
  ↓
向 TUI 返回状态
```

四个概念的区别：

```text
Workflow：规定任务大体怎么流转。
Loop：让某些步骤重复。
Agent：根据上下文动态决定下一步。
Runtime：负责真正调度状态、LLM、工具、错误与停止。
```

Agent Loop 可以是 Workflow 中的一个步骤。

---

## 12. 当前项目相对 S0 的演进

当前仓库已经在 S0 地基上扩展出：

| 当前能力 | 相关模块 |
|---|---|
| Anthropic LLM | `core/llm/` |
| Agent Plan→Act→Observe | `core/loop.py` |
| Agent Run 编排 | `core/runner.py` |
| 工具注册与执行 | `core/tools/` |
| Session 持久化 | `core/session/` |
| Task 管理 | `core/task/` |
| 事件总线和推送 | `core/events/`、`ipc_broadcaster.py` |
| 权限审批 | `core/permissions/` |
| Trace | `core/trace/` |
| 上下文压缩 | `core/compact/` |
| 子 Agent | `core/subagent/` |
| MCP | `core/mcp/` |
| Textual TUI | `tui/` |

当前 Core 注册的方法包括：

```text
core.ping
agent.run
event.subscribe
session.create
session.send_message
session.get_history
session.close
permission.respond
session.compact
```

所以“daemon 只回答 `core.ping`”是 S0 的历史结论，不是当前仓库现状。

---

## 13. 工程质量与协议同步

代码检查：

```bash
uv run ruff check src tests scripts
uv run mypy src
```

测试：

```bash
uv run pytest tests/unit -v
uv run pytest tests/integration -v
uv run pytest tests/ -v
```

修改 Wire 模型后重新生成协议文档：

```bash
uv run python scripts/gen_protocol_doc.py
```

检查是否同步：

```bash
uv run python scripts/gen_protocol_doc.py --check
```

原则：

```text
Pydantic 模型是协议事实来源
        ↓
脚本生成 WIRE_PROTOCOL.md
        ↓
避免人工维护两份容易漂移的定义
```

---

## 14. 最终心智模型

阅读 ScottClaude 时，可以沿着下面的链路定位：

```text
入口层
pyproject.toml → CLI/TUI/Core
        ↓
配置层
ScottConfig → TOML/.env/环境变量
        ↓
运行层
asyncio.run → Event Loop → CoreApp
        ↓
传输层
TCP → StreamReader/Writer → NDJSON
        ↓
协议层
JsonRpcRequest/Success/Error → Pydantic
        ↓
路由层
method → SocketServer.register() → handler
        ↓
业务层
Ping / Session / Agent / Permission / Event
        ↓
Agent Runtime
LLM → Tool → Observation → 下一轮
        ↓
持久化与可观测
Event / Trace / Session / Task
```

最值得记住的六句话：

1. `scott-core` 是常驻执行中心，TUI/CLI 是客户端。
2. TCP 提供字节流，NDJSON 提供消息边界，JSON-RPC 提供消息语义。
3. Pydantic 负责校验和序列化，不负责网络传输。
4. `method` 选择 RPC handler，`type` 标识业务模型类型。
5. Event Loop 调度协程，Agent Loop 驱动 LLM 与工具反复协作。
6. Workflow 规定任务流转，Agent Runtime 负责把整个过程真正运行起来。

### 建议阅读顺序

1. `pyproject.toml`
2. `src/scott_claude/cli/main.py`
3. `src/scott_claude/cli/commands/ping.py`
4. `src/scott_claude/core/config.py`
5. `src/scott_claude/core/bus/envelope.py`
6. `src/scott_claude/core/bus/commands.py`
7. `src/scott_claude/core/transport/socket_server.py`
8. `src/scott_claude/core/app.py`
9. `src/scott_claude/core/runner.py`
10. `src/scott_claude/core/loop.py`
11. `src/scott_claude/core/tools/`
12. `src/scott_claude/core/events/`
